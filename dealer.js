class Dealer{
constructor(x,y){
getHand = hand;
addCardToHand = card;
this.x = x;
this.y = y;
}

// der dannes en if statement for at bestemme om dealer skal "hit" eller ej'
/* if (totalValue() <= 17){
dealer.hit()
}
else if (totalValue() > 17){
dealer.stand()

}
*/
}