class Player {
  constructor(x, y, getHand) {
    this.x = X;
    this.y = Y;
    this.getHand = hand.hand;
    this.score = 0;
  }

  update() {
    if (this.hit == true) {
      this.getHand = this.getHand + addCardToHand;
      this.getHand.push(card);
    }
  }
}
